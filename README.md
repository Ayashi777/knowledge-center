# Корпоративний Центр Знань

Це веб-додаток, розроблений як централізоване сховище для внутрішніх документів компанії. Він дозволяє співробітникам знаходити, переглядати та керувати документацією відповідно до їхніх ролей та прав доступу.

Переглянути додаток в AI Studio: https://ai.studio/apps/drive/1Pb3wH165_wLA4rwLTNrC-YIu6lneKj30

## 🚀 Основні можливості

*   **Система ролей:** Різні рівні доступу для гостей, прорабів, дизайнерів та адміністраторів.
*   **Фільтрація та пошук:** Потужні інструменти для швидкого пошуку документів за назвою, категорією або тегами.
*   **Сортування та режими перегляду:** Можливість сортувати документи (за датою, за алфавітом) та відображати їх у вигляді списку або сітки.
*   **Багатомовність:** Підтримка української та англійської мов.
*   **Адаптивний дизайн:** Зручний перегляд на будь-яких пристроях.
*   **Темна/Світла тема:** Можливість перемикання теми інтерфейсу.
*   **Керування документами:** Адміністратори можуть додавати, редагувати та видаляти документи.

## 🛠️ Технологічний стек

*   **Фреймворк:** React 19
*   **Збирач:** Vite
*   **Мова:** TypeScript
*   **Стилізація:** Tailwind CSS
*   **Інтернаціоналізація (i18n):** Власна реалізація з `useI18n` хуком.

## 📂 Структура проєкту

Проєкт має компонентну архітектуру. Основна логіка знаходиться у файлі `App.tsx`, але для подальшого розвитку рекомендується наступна структура:

```
/src
├── components/      # Загальні компоненти (кнопки, іконки, модальні вікна)
│   ├── auth/
│   ├── documents/
│   └── layout/
├── hooks/           # Кастомні хуки (наприклад, useI18n)
├── pages/           # Компоненти сторінок (Dashboard, DocumentView)
├── services/        # Логіка для роботи з API
├── styles/          # Глобальні стилі
├── types/           # Глобальні типи TypeScript
├── utils/           # Допоміжні функції
├── constants.ts     # Константи та мокові дані
├── i18n.tsx         # Логіка інтернаціоналізації
├── translations.ts  # Файли перекладів
└── main.tsx         # Вхідна точка додатку
```

## 🏃‍♀️ Запуск локально

**Передумови:** Встановлений [Node.js](https://nodejs.org/).

1.  **Встановіть залежності:**
    ```bash
    npm install
    ```

2.  **Запустіть сервер для розробки:**
    ```bash
    npm run dev
    ```
    Додаток буде доступний за адресою `http://localhost:3000` (або на іншому порту, якщо 3000 зайнятий).

## 📜 Доступні скрипти

*   `npm run dev`: Запускає додаток в режимі розробки з гарячим перезавантаженням.
*   `npm run build`: Збирає додаток для продакшену в папку `dist`.
*   `npm run preview`: Запускає локальний сервер для перегляду продакшен-збірки.

## 📝 Рекомендації з аудиту

На основі проведеного аналізу коду було виявлено наступні напрямки для покращення:

1.  **Рефакторинг `App.tsx`:** Розділити масивний компонент `App` на менші, більш керовані компоненти та рознести їх по відповідних папках (`pages`, `components`).
2.  **Управління станом:** Для складних станів розглянути можливість впровадження бібліотеки управління станом (наприклад, Redux Toolkit, Zustand), щоб уникнути "прокидування" пропсів (prop drilling).
3.  **Шар для API:** Створити окремий шар `services` для роботи з API, щоб відокремити логіку отримання даних від компонентів.
4.  **Якість коду:** Інтегрувати лінтер (ESLint) та форматер (Prettier) для підтримки єдиного стилю коду.
5.  **Тестування:** Додати фреймворк для тестування (наприклад, Vitest, Jest) для написання unit- та інтеграційних тестів.
6.  **Доступність (Accessibility):** Додати `alt` атрибути для зображень та перевірити доступність інтерактивних елементів з клавіатури.
7.  **Безпека:** Використовувати бібліотеку (наприклад, `dompurify`) для санітизації HTML, що генерується з контенту документів, для запобігання XSS-атак.
