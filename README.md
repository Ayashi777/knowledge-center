# Corporate Knowledge Center 📚

Професійний корпоративний портал бази знань з підтримкою рівнів доступу, мультимовністю та сучасним інтерфейсом.

## 🚀 Основні можливості

*   **Система ролей (RBAC):** Різні рівні доступу для гостей, прорабів, дизайнерів та адміністраторів.
*   **Адмін-панель:** Керування матрицею доступів до категорій та документів у реальному часі.
*   **Фільтрація та пошук:** Потужний пошук за назвою, категорією або тегами.
*   **Сортування та режими перегляду:** Перемикання між списком та сіткою, сортування за датою або алфавітом.
*   **Багатомовність:** Повна підтримка української та англійської мов (i18n).
*   **Темна/Світла тема:** Адаптивний інтерфейс з підтримкою системних налаштувань.
*   **Керування контентом:** Редагування текстового наповнення документів безпосередньо в браузері (для адмінів).

## 🛠️ Технологічний стек

*   **Frontend:** React 19, TypeScript
*   **Build Tool:** Vite
*   **Styling:** Tailwind CSS
*   **Deployment:** Firebase Hosting
*   **Icons:** Heroicons (custom SVG implementation)

## 📂 Структура проєкту та призначення файлів

Проєкт розбитий на логічні модулі для полегшення підтримки та масштабування:

### 🏗️ Основні файли
*   `App.tsx` — Головний компонент, точка входу в UI логіку. Керує глобальним станом (ролі, тема, обраний документ).
*   `index.tsx` — Рендеринг React додатку.
*   `types.ts` — Визначення всіх TypeScript інтерфейсів (Document, Category, UserRole).
*   `constants.ts` — Початкові дані: список документів та категорій.
*   `translations.ts` — Словник перекладів для обох мов.
*   `i18n.tsx` — Контекст та хук `useI18n` для керування локалізацією.

### 🧩 Компоненти (`/components`)
*   `AdminPanel.tsx` — Ексклюзивний інтерфейс адміністратора. Містить матрицю дозволів для ролей.
*   `Sidebar.tsx` — Бічна панель з фільтрами за категоріями та тегами.
*   `Views.tsx` — Основні представлення: `DashboardView` (головна сторінка) та `DocumentView` (сторінка документа).
*   `Modals.tsx` — Набір модальних вікон: логін, реєстрація, редагування документів та категорій.
*   `DocumentComponents.tsx` — Картки документів (`GridItem`, `ListItem`) та ескізи (`Thumbnail`).
*   `UI.tsx` — Дрібні елементи керування: перемикачі мов, тем, поля вводу.
*   `icons.tsx` — Централізована бібліотека SVG іконок.

### ⚓ Хуки та Утиліти
*   `hooks/useDocuments.ts` — Вся бізнес-логіка фільтрації, пошуку, сортування та пагінації.
*   `utils/format.ts` — Допоміжні функції, наприклад, форматування відносного часу ("щойно", "2 дні тому").

## ⚙️ Логіка взаємодії

1.  **Доступ:** Додаток починає роботу в ролі `guest`. Гості бачать лише загальну інформацію. При спробі переглянути обмежений контент з'являється форма запиту доступу.
2.  **Авторизація:** Емульована система входу дозволяє перемикатися між ролями для тестування логіки доступу.
3.  **Адміністрування:** При виборі ролі `admin`, додаток підсвічується синьою рамкою, з'являються кнопки редагування біля кожного документа та іконка налаштувань у хедері.
4.  **Матриця доступів:** В адмін-панелі можна вказати, які ролі мають право бачити конкретну категорію. Зміни миттєво оновлюють `visibleCategories` у стані додатка.

## 🚀 Деплой

Проєкт налаштований для автоматичного розгортання у Firebase Hosting:

```bash
# Встановити залежності
npm install

# Запустити в режимі розробки
npm run dev

# Білд та деплой на Firebase
npm run deploy
```

Конфігураційні файли `firebase.json` та `.firebaserc` вже містять налаштування для проекту `knowledge-center-5274257-2457a`.
